# Production Environment

This environment is intentionally a placeholder.

The production target has not been selected yet. When it is, add the provider-specific configuration here, for example:

- managed Kubernetes on a cloud provider
- Kubernetes on virtual machines
- a single production VM running a lightweight Kubernetes distribution

Do not commit production Terraform state or secrets to this directory.
