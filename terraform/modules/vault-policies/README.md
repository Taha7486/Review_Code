# Vault Policies Module

Defines least-privilege Vault policies for CodeReview service secrets.

- `codereview-dotnet-api` can read `secret/data/codereview/dotnet-api/*`.
- `codereview-php-service` can read `secret/data/codereview/php-service/internal-bridge`.
